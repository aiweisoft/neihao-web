# 医疗设备管理系统初始化说明

## 已创建的文件

### 1. 数据库表结构
- `medical-device.schema.json` - 医疗设备台账表
- `medical-device-category.schema.json` - 设备分类表
- `medical-device-location.schema.json` - 设备位置表

### 2. 初始化数据
- `medical-device.init_data.json` - 设备示例数据（5条记录）
- `medical-device-category.init_data.json` - 设备分类数据（10条记录）
- `medical-device-location.init_data.json` - 设备位置数据（5条记录）

### 3. 数据库初始化配置
- `db_init.json` - 数据库初始化配置文件（包含菜单配置）

### 4. 前端页面
- `pages/medical-device/list.vue` - 设备列表页面
- `pages/medical-device/add.vue` - 新增设备页面
- `pages/medical-device/edit.vue` - 编辑设备页面

## 初始化步骤

### 步骤1：初始化数据库
1. 在 HBuilderX 中打开项目
2. 右键点击 `uniCloud-alipay/database/db_init.json` 文件
3. 选择 "初始化云数据库"
4. 系统会自动创建所有表并插入初始化数据

### 步骤2：运行项目
1. 在 HBuilderX 中点击 "运行" -> "运行到浏览器"
2. 登录系统后，左侧菜单会出现 "医疗设备管理" 菜单项
3. 包含子菜单：设备台账、设备分类、设备位置

## 表结构说明

### 1. medical-device（设备台账表）
| 字段 | 类型 | 说明 |
|------|------|------|
| code | string | 设备编号（唯一） |
| name | string | 设备名称 |
| short_name | string | 设备简称 |
| brand | string | 品牌 |
| model | string | 型号 |
| spec | string | 规格 |
| serial_no | string | 序列号 |
| category_id | string | 设备分类ID |
| dept_id | string | 使用部门ID |
| location_id | string | 存放位置ID |
| purchase_date | timestamp | 采购日期 |
| purchase_amount | double | 采购金额 |
| supplier | string | 供应商 |
| warranty_end | timestamp | 保修期截止 |
| status | int | 使用状态（1-正常,2-使用中,3-维修中,4-报废） |
| image_url | string | 设备照片URL |
| qr_code | string | 二维码（唯一） |
| remark | string | 备注 |
| deleted | int | 软删除标记（0-否,1-是） |

### 2. medical-device-category（设备分类表）
| 字段 | 类型 | 说明 |
|------|------|------|
| code | string | 分类编码（唯一） |
| name | string | 分类名称 |
| parent_id | string | 父级分类ID |
| sort | int | 排序号 |

### 3. medical-device-location（设备位置表）
| 字段 | 类型 | 说明 |
|------|------|------|
| code | string | 位置编码（唯一） |
| name | string | 位置名称 |
| dept_id | string | 所属部门ID |
| building | string | 楼栋 |
| floor | string | 楼层 |
| room | string | 房间号 |

## 功能特性

1. **设备管理**
   - 设备信息的增删改查
   - 设备状态管理（正常、使用中、维修中、报废、未投入、其他）
   - 设备照片上传
   - 二维码管理

2. **分类管理**
   - 多级设备分类
   - 分类排序

3. **位置管理**
   - 设备存放位置管理
   - 按部门、楼栋、楼层、房间号管理

4. **数据导出**
   - 支持Excel导出
   - 批量操作

## 注意事项

1. 设备编号和二维码字段是唯一的，不能重复
2. 使用软删除机制，删除的数据标记为 deleted=1
3. 所有表都有创建时间、更新时间、创建人、更新人字段
4. 支持权限控制，需要配置相应的权限才能操作

## 后续扩展建议

1. **设备维修记录** - 记录设备的维修历史
2. **设备保养计划** - 制定设备保养计划
3. **设备借用管理** - 设备借用和归还管理
4. **设备折旧计算** - 自动计算设备折旧
5. **设备报警提醒** - 保修期、保养期提醒
6. **移动端扫码** - 通过手机扫码查看设备信息

## 技术支持

如有问题，请参考：
1. uniCloud 官方文档：https://uniapp.dcloud.io/uniCloud/
2. uni-admin 官方文档：https://uniapp.dcloud.io/uniCloud/admin.html
3. 项目 GitHub：https://github.com/dcloudio/uni-admin